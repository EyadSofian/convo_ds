# dependencies
node_modules/
.pnpm-store/
.venv/
__pycache__/

# build output
dist/
build/
.turbo/
*.tsbuildinfo

# test + evidence artifacts (generated, not committed raw)
coverage/
reports/
.stryker-tmp/
playwright-report/
test-results/
.tmp/
tmp/
output/
docs/evidence/**/raw/

# Owner-only project review: this repository is public. Never publish the
# technical risk register or operational evidence with the client portal.
docs/project/PROJECT_STATUS_INTERNAL.md

# local runtime state
.env
.env.*
!.env.example
*.local
.demo-staging-credentials.txt
.demo-staging-cookiejar.txt
.data/
pgdata/

# research cache: a 22MB read-only clone of chatwoot at a pinned commit, used as
# reference evidence only. Not part of this product's source. See docs/research/.
.research-cache/

# os
.DS_Store

# generated build output of workspace packages
packages/*/dist/
